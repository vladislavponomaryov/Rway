let initialState = {
    friends: [
        {name: 'Andrew'},
        {name: 'Sasha'},
        {name: 'Alexander'}
    ]
}

const sidebarReducer = (state = initialState) => {

    return state;
}

export default sidebarReducer;